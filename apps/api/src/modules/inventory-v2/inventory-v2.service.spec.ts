import { movementTotalCostMinor, projectBalanceMilli, weightedAverageCostMinor } from './inventory-v2.service'; describe('append-only inventory accounting',()=>{it('projects balance only from movements',()=>expect(projectBalanceMilli([{quantityMilli:5000},{quantityMilli:-1250},{quantityMilli:250}])).toBe(4000));it('updates weighted average cost after receiving',()=>expect(weightedAverageCostMinor({currentMilli:10000,currentCostMinor:200,receivedMilli:10000,receivedCostMinor:400})).toBe(300));it('snapshots COGS in integer minor units',()=>expect(movementTotalCostMinor(-2500,199)).toBe(498));});
