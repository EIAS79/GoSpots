import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function OperationsPage() {
  return (
    <PlaceholderPage
      title="Operations"
      description="Live floor view — tables, timers, and session controls. The core product screen."
    />
  );
}
