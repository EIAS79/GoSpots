# GoSpots brand assets

Drop your files here (replace the placeholders):

| File | Purpose |
|------|---------|
| `gospots-logo.png` | **Horizontal** lockup: icon on the left + **GoSpots** wordmark. Navbar, footer, auth, dashboard. Prefer transparent PNG, ~880×200 or similar wide ratio. |
| `gospots-icon.png` | Square icon only. Browser tab + compact `markOnly` UI. |
| `gospots-og.png` | Optional social / Open Graph image (~1200×630). |

**Also replace** the tab favicon copy used by Next.js:

`apps/web/src/app/icon.png` ← same square icon as `gospots-icon.png`

Public URLs:

- `/brand/gospots-logo.png`
- `/brand/gospots-icon.png`
- `/brand/gospots-og.png`

The UI sizes the wordmark generously (~36–56px tall, ~4.4∶1 width) and does **not** render a second “GoSpots” text next to the image.
