# GoSpots Phase 14 — Analytics, Reconciliation and Operational Intelligence

## Status

`ACCEPTED`

Phase 14 is implemented, tested, merged and production-validated. The remaining Phase 14.11 production-like database benchmark gap was closed by PR #78 and permanently added to the Phase 14 CI gate. Phase 15 has not been started.

## Source

GoSpots Master Product & Engineering Execution Plan v2 — Phase 14.

## Scope decisions

- Reuse `GrowthAnalyticsService` as the mature source for canonical finance, operations and customer analytic projections.
- Reuse Phase 5 `FinancialReconciliationRun/FinancialReconciliationIssue` as persisted reconciliation evidence.
- Add a Phase 14 owner read model rather than a second financial or inventory source of truth.
- No database schema change is required for Phase 14. Existing canonical and reconciliation schemas already contain the necessary facts. Consequently there is no Phase 14 migration; CI still deploys and validates the full clean migration chain.
- Preserve the pre-Phase-14 analytics workspace for backward compatibility and place the owner intelligence/reconciliation surface above it.

## Acceptance matrix

| Requirement | Evidence |
|---|---|
| Metric dictionary | `phase14-metric-dictionary.ts`, `docs/analytics/METRIC_DICTIONARY.md`, dictionary Jest gate |
| Financial KPIs | Phase14 owner service `financial()` over Ledger/Settlement/Payment/Cash/adjustment facts |
| Resource KPIs | Phase14 owner service `resources()` over canonical operations + opening/maintenance facts |
| Restaurant KPIs | `restaurant()` over VenueOrder/Line, GuestCheck profile, KDS and comp evidence |
| Inventory KPIs | `inventory()` over immutable StockMovement + StockItem costing configuration |
| Reservation KPIs | `reservations()` over Reservation/extension/deposit/waitlist/session facts |
| Customer KPIs | `customers()` over CustomerVisit, CustomerMembership, loyalty and stored-value ledgers |
| Workforce KPIs | `workforce()` over TimePunch, StaffActionEvidence, CashSession and operator sales |
| Reconciliation Center | persisted Phase 5 reconciliation + Phase 14 live invariants with severity/amount/entity/time/action/evidence-link metadata, surfaced in the owner UI |
| Attention Center | owner UI + cross-domain derived attention items with evidence links when a canonical entity is available |
| Production-sized synthetic performance | `phase14-performance-benchmark.ts`: 370,000 facts (250,000 finance + 120,000 resource), 200 resources; final closeout run 100.6 ms against a 5,000 ms budget |
| Production-like PostgreSQL performance | `phase14-database-performance-benchmark.ts`: real PostgreSQL 17, all 115 migrations, 285,000 explicit fixture rows plus canonical ledger side effects, real `Phase14AnalyticsService.workspace()` path, tenant-first index assertions and exact result assertions; 4,944.6 ms against a 15,000 ms workspace budget |
| DST/business day | `phase14-business-day.spec.ts` proves Warsaw spring/fall DST, 04:00 overnight boundary and different branch settings |
| Same-event consistency | `phase14-integrity-assert.ts` creates one cash sale and proves checkout=payment=ledger=cash=finance report=analytics plus tenant isolation |
| Empty/large range | API supports 1..370 venue business dates; empty datasets return zero/null projections without inventing totals |
| Tenant scope | every Phase 14 database query derives `shopId` from authenticated `JwtAccessPayload`; integrity and DB benchmark fixtures prove cross-shop exclusion |

## Exact-head verification evidence

Phase 14 performance closeout candidate:

- tested head: `8522181ee3c10626d039f1135e9f41a1d0a9c2a2`
- dedicated Phase 14 workflow: `32255291169` — PASS
- generic CI: `32255291117` — PASS
- focused semantic tests: 4 suites / 15 tests — PASS
- API build — PASS
- web typecheck — PASS
- web production build — PASS
- permanent browser E2E suite — PASS
- clean PostgreSQL 17 migration chain: 115 migrations — PASS
- representative historical-data migration upgrade — PASS
- Phase 3/4/7/8/9/10/11/13 regression workflows — PASS
- standalone product boundary — PASS
- Edge hard-outage validation — PASS

Canonical Gate P14 fixture:

- checkout: `100.0000`
- payment: `100.0000`
- ledger: `100.0000`
- cash: `100.0000`
- finance report: `100.0000`
- analytics: `100.0000`
- tenant isolation: `true`

Production-like PostgreSQL benchmark fixture:

- explicit target-tenant ledger facts: 200,000
- GuestChecks: 25,000
- CheckSettlements: 25,000
- successful Payments: 25,000
- explicit other-tenant ledger facts: 10,000
- explicit fixture rows: 285,000
- canonical ledger side-effect rows generated by the migrated financial model: 25,000, tracked separately from explicit benchmark facts
- tenant-first index coverage: LedgerEntry = true, Payment = true, CheckSettlement = true
- seed: 31,180.2 ms <= 120,000 ms budget
- real owner workspace/read-model execution: 4,944.6 ms <= 15,000 ms budget
- exact result: grossSalesMinor = 25,000,000; netSalesMinor = 25,000,000; cashTenderMinor = 25,000,000; cashTenderCount = 25,000; returned tenant = benchmark tenant

## Merge and production evidence

Performance closeout PR #78 was merged with expected-head SHA protection.

- merge/main SHA: `05b12ecc4e86e2a46b8a5cd8caf5415555dbbadb`
- Vercel production deployment: `dpl_6oYkoUYqk6JJS92EE61ntjdNTeHJ`
- deployment git SHA: `05b12ecc4e86e2a46b8a5cd8caf5415555dbbadb`
- target: production
- deployment state: `READY`
- `https://www.gospots.pl/`: HTTP 200
- unauthenticated `GET /api/v1/growth/analytics/phase14/metrics`: HTTP 401 `Unauthorized`, proving the deployed route exists and its authentication guard is active; response traversed the production Render backend
- Vercel runtime error/fatal query for this production deployment: zero matching logs

## Security

- Endpoints require `JwtAuthGuard` and `report.read`.
- Client input never supplies a trusted shop/tenant ID.
- Phase 14 performs read-only analytics/reconciliation projection; no refund, cash, inventory, permission or financial correction is performed from analytics.

## Rollout and rollback

The change is schema-neutral. Rollback is application-only: revert the Phase 14 API/UI commits. No canonical business fact needs reversal and no database column/table is removed.

## Stop boundary

Phase 15 automation/AI is outside this work. Phase 14 exposes evidence that Phase 15 can consume later, but it does not execute rules, send notifications, or add AI behavior.
